import 'package:flutter/material.dart';

class CheckboxRow extends StatelessWidget {
  const CheckboxRow({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO 2.1 CheckBoxRow
    return Container();
  }
}
